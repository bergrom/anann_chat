# 好友聊天

用户建立双向好友关系，并通过单聊交换文字消息。

## Language

**用户名（Username）**：
用户的唯一且不可修改的账号标识，用于登录、精确查找其他用户和身份展示。
_Avoid_：昵称、显示名

**好友申请（Friend Request）**：
一个用户向另一个用户提出建立好友关系的请求，状态为待处理、已接受或已拒绝；接收方接受后，两人成为好友。
_Avoid_：关注请求

**好友关系（Friendship）**：
由好友申请获同意而建立的双向关系；双方互为好友，并出现在彼此的好友列表中。
_Avoid_：关注关系、单向联系人

**好友列表（Friend List）**：
与当前用户已建立好友关系的用户集合，不包含尚未获同意的申请对象。

**单聊（Direct Conversation）**：
同一对用户共用的唯一私聊会话，保留双方的消息历史；仅当两人是好友时，才允许发送消息。
_Avoid_：群聊

**最近会话列表（Recent Conversations）**：
按最新消息时间排序的单聊集合，每项展示最后一条消息及当前用户的未读消息数。
_Avoid_：好友列表

**未读消息（Unread Message）**：
对方发来的、当前用户尚未在可见聊天中查看的消息；未读数量仅供当前用户使用，不向发送方提供已读回执。

**文字消息（Text Message）**：
单聊中由一名用户发送给另一名用户的文本内容，不包含图片、文件或音视频。
